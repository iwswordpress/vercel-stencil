{
  "name": "stencil-cw.vercel.app",
  "private": true,
  "version": "1.0.0",
  "description": "Dev site to see components in app and deployed to vercel. These components are then used in npm-*** build projects.",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "dependencies": {
    "@stencil/core": "^1.3.3",
    "@stencil/router": "^1.0.1"
  },
  "license": "MIT"
}
